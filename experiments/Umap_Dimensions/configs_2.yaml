reducers:
  umap-2-all:
    algorithm: umap
    kwargs:
      n_components: 2
    windowed:
      fit_on: null
      transform_on: all

  umap-3-all:
    algorithm: umap
    kwargs:
      n_components: 3
    windowed:
      fit_on: null
      transform_on: all

  umap-4-all:
    algorithm: umap
    kwargs:
      n_components: 4
    windowed:
      fit_on: null
      transform_on: all

  umap-5-all:
    algorithm: umap
    kwargs:
      n_components: 5
    windowed:
      fit_on: null
      transform_on: all

  umap-6-all:
    algorithm: umap
    kwargs:
      n_components: 6
    windowed:
      fit_on: null
      transform_on: all

  umap-7-all:
    algorithm: umap
    kwargs:
      n_components: 7
    windowed:
      fit_on: null
      transform_on: all

  umap-8-all:
    algorithm: umap
    kwargs:
      n_components: 8
    windowed:
      fit_on: null
      transform_on: all

  umap-9-all:
    algorithm: umap
    kwargs:
      n_components: 9
    windowed:
      fit_on: null
      transform_on: all

  umap-10-all:
    algorithm: umap
    kwargs:
      n_components: 10
    windowed:
      fit_on: null
      transform_on: all

estimators:
  randomforest-100:
    algorithm: RandomForest
    kwargs:
      n_estimators: 100
    allow_multirun: true

transformers:
  no_transform:
    - transform: identity

  with_fft_all:
    - transform: fft
      kwargs:
        centered: True
      windowed:
        fit_on: null
        transform_on: all
